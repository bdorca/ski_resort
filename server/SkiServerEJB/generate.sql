--<ScriptOptions statementTerminator=";"/>

CREATE TABLE USER.LIFTVALUE (
		TYPE INTEGER NOT NULL,
		MAXRESOURCE DOUBLE NOT NULL,
		MINRESOURCE DOUBLE NOT NULL,
		NAME VARCHAR(255)
	);

CREATE TABLE USER.USERDATA (
		USERNAME VARCHAR(255) NOT NULL,
		PASSWORD VARCHAR(255),
		TOKEN VARCHAR(255)
	);

CREATE TABLE USER.LIFT (
		ID INTEGER NOT NULL,
		ADD_PEOPLE DOUBLE NOT NULL,
		CONSUMPTION DOUBLE NOT NULL,
		CUSTOMERS DOUBLE NOT NULL,
		FAILURE DOUBLE NOT NULL,
		LIFTID VARCHAR(255),
		NAME VARCHAR(255),
		RESOURCE DOUBLE NOT NULL,
		SPEED DOUBLE NOT NULL,
		TYPE INTEGER,
		VALUES_TYPE INTEGER,
		RUNNING INT DEFAULT 1
	);

CREATE UNIQUE INDEX USER.SQL161127113511521 ON USER.LIFTVALUE (TYPE ASC);

CREATE UNIQUE INDEX USER.SQL161127225818170 ON USER.USERDATA (USERNAME ASC);

CREATE INDEX USER.SQL161127113512280 ON USER.LIFT (VALUES_TYPE ASC);

CREATE UNIQUE INDEX USER.SQL161127113511170 ON USER.LIFT (ID ASC);

ALTER TABLE USER.USERDATA ADD CONSTRAINT SQL161127225818170 PRIMARY KEY (USERNAME);

ALTER TABLE USER.LIFT ADD CONSTRAINT SQL161127113511170 PRIMARY KEY (ID);

ALTER TABLE USER.LIFTVALUE ADD CONSTRAINT SQL161127113511520 CHECK ((type<=3 AND type>=0));

ALTER TABLE USER.LIFTVALUE ADD CONSTRAINT SQL161127113511521 PRIMARY KEY (TYPE);

ALTER TABLE USER.LIFT ADD CONSTRAINT FKHIE380ISTGBEUXX6685VYTNNE FOREIGN KEY (VALUES_TYPE)
	REFERENCES USER.LIFTVALUE (TYPE);

insert into user.lift values (0,120.0,30.0,'Gyakorlo Lift');
insert into user.lift values (1,207.0,30.0,'Kozephalado Lift');
insert into user.lift values (2,377.0,30.0,'Halado Lift');
insert into user.lift values (3,671.0,30.0,'Profi lift');

